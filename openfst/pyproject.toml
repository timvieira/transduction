[build-system]
requires = ["setuptools>=64", "wheel", "cython>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "openfst-decomp"
version = "0.1.0"
description = "OpenFST-backed decomposition backend for transduction"
requires-python = ">=3.10"
